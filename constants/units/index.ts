import COST from './COST';
import RANK from './RANK';
import SALARY from './SALARY';
import XP_PROMOTION from './XP_PROMOTION';

const UNITS = { COST, RANK, SALARY, XP_PROMOTION };

export default UNITS;
